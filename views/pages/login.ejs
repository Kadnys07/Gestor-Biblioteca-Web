<div class="w-full max-w-md p-8 space-y-8 bg-white rounded-xl shadow-lg mx-auto mt-24">
  <h2 class="text-3xl font-bold text-center text-gray-800">Gestor de Biblioteca</h2>
  <p class="text-center text-gray-500">Faça login para continuar</p>
  <% if (erro) { %>
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-2 rounded text-center">
      <%= erro %>
    </div>
  <% } %>
  <form id="login-form" class="space-y-6" method="POST" action="/login">
    <div>
      <label for="email" class="text-sm font-medium text-gray-700">Email</label>
      <input id="email" name="email" type="email" required class="w-full px-3 py-2 mt-1 border border-gray-300 rounded-md shadow-sm focus:ring-green-500 focus:border-green-500" placeholder="gestor@biblioteca.com">
    </div>
    <div>
      <label for="senha" class="text-sm font-medium text-gray-700">Senha</label>
      <!-- ARQUITETO: Removido o atributo 'value' para garantir que o utilizador insira a senha correta. -->
      <input id="senha" name="senha" type="password" required class="w-full px-3 py-2 mt-1 border border-gray-300 rounded-md shadow-sm focus:ring-green-500 focus:border-green-500">
    </div>
    <button type="submit" class="w-full px-4 py-2 font-semibold text-white bg-green-700 rounded-md hover:bg-green-800 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500 transition-colors">
      Entrar
    </button>
  </form>
</div>

